name: Playlist CI/CD

on:
  push:
    branches: ["main"]
    paths:
      - "apps/playlist/**"
      - "libs/**"
      - ".github/workflows/playlist-service.yml"
      - "package.json"
      - "yarn.lock"
      - "jest.config.json"
      - "eslint.config.js"
      - "nest-cli.json"
      - "tsconfig.json"
      - "tsconfig.build.json"
  pull_request:
    branches: ["main"]
    paths:
      - "apps/playlist/**"
      - "libs/**"
      - ".github/workflows/playlist-service.yml"
      - "package.json"
      - "yarn.lock"
      - "jest.config.json"
      - "eslint.config.js"
      - "nest-cli.json"
      - "tsconfig.json"
      - "tsconfig.build.json"

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint all the changes made in the libraries and playlist

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"

      - name: Install Dependencies (Root)
        run: yarn install --frozen-lockfile

      - name: Generate Prisma Client (playlist)
        run: npx prisma generate --schema=apps/playlist/prisma/schema.prisma

      - name: Run linter (libraries)
        run: yarn lint:libs

      - name: Run linter (playlist)
        run: yarn lint:playlist

  test-and-validate:
    runs-on: ubuntu-latest
    needs: lint
    name: Tests and validates all the changes before building and pushing the image

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"

      - name: Install Dependencies (Root)
        run: yarn install --frozen-lockfile

      - name: Generate Prisma Client (playlist)
        run: npx prisma generate --schema=apps/playlist/prisma/schema.prisma

      - name: Run Unit Tests (playlist)
        run: npx jest apps/playlist/test/unit

      - name: Run Integration Tests (playlist)
        run: npx jest apps/playlist/test/integration

  build-and-push:
    name: Build & Push to Docker Hub
    needs: test-and-validate
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/playlist/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/streamforge-playlist:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/streamforge-playlist:${{ github.sha }}
